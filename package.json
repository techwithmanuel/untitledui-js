{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsup",
    "publish": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.9.2",
    "esbuild-svelte": "^0.9.3",
    "unplugin-vue": "^7.0.1"
  }
}
